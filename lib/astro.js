export async function computeAT(stockName) {
  // placeholder for now (structured)
  // will plug Swiss Ephemeris later

  const base = 6 + Math.random() * 2;

  return parseFloat(base.toFixed(2));
}
